spring:
    config:
        import: security-variable.yml
    application:
        name: ShonGram
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/shongram
        username: root
        password: ${mysql.password}